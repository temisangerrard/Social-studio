{
  "name": "peppera-slideshow-factory",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "Deterministic slideshow generation pipeline for Peppera vertical social posts — with web frontend.",
  "scripts": {
    "generate": "tsx src/index.ts",
    "server": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test src/assistant.test.ts src/context.test.ts src/image-generator.test.ts src/pipeline.test.ts src/planner.test.ts src/server.test.ts src/storage.test.ts src/workflow-engine.test.ts public/app-helpers.test.ts"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.52.0",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "fast-check": "^4.7.0",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
